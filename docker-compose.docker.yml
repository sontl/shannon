# Docker-specific overrides (not used with Podman)
# This file is automatically included by the gandalf script when running Docker
services:
  worker:
    extra_hosts:
      - "host.docker.internal:host-gateway"
